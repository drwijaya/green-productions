# Nixpacks configuration for Railway
# Installs LibreOffice for Word-to-PDF conversion

[phases.setup]
nixPkgs = ["python311", "libreoffice", "gcc"]

[phases.install]
cmds = ["python3 -m ensurepip --default-pip", "python3 -m pip install -r requirements.txt"]
