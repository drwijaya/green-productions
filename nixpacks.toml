# Nixpacks configuration for Railway
# Installs LibreOffice for Word-to-PDF conversion

[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "libreoffice", "gcc"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]
